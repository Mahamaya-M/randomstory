

You can open these files in any editor
and start writing.
*Note: You can be interactive with the
student and create a new story with
different characters based on student
inputs.
Let's initialize git using 'git init' so that
git can start tracking changes in our